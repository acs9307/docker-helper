# Copy to .env and edit values; all variables are optional unless noted.

# Scanner cadence (seconds) and severity filter
# SCAN_INTERVAL_SECONDS=86400
# TRIVY_SEVERITY=HIGH,CRITICAL

# Optional Trivy flags (examples)
# TRIVY_EXTRA_ARGS=--ignore-unfixed
# TRIVY_EXTRA_ARGS=--timeout 15m

# Snapshot/whitelist settings
# SNAPSHOT_WRITE=0               # set to 1 to write/update snapshot after scan
# SNAPSHOT_DIR=/snapshots

# Limit scanned images
# SCAN_IMAGES=                   # comma-separated list (e.g., alpine:3.18,ubuntu:22.04). If empty, all images are scanned.

# Ignore list
# IGNORE_FILE=/ignore-images.txt   # path in container to a line-delimited ignore list (default /ignore-images.txt)
# IGNORE_IMAGES=                   # fallback comma-separated list if no file is provided

# SMTP settings for email alerts (set here or via secrets files in the secrets directory of the same name)
# SMTP_HOST=smtp.example.com          # required to send mail
# SMTP_PORT=587
# SMTP_USER=scanner
# SMTP_PASS='changeme'
# SMTP_FROM=scanner@example.com
# SMTP_TO=admin@example.com           # required to send mail
# SMTP_TLS=true
# SMTP_STARTTLS=true
# SMTP_SUBJECT_PREFIX=[Image Scanner]
